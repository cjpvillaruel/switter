<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>


    <script>
      function go() {
        var txt=document.getElementById("txt");
        txt.onkeydown = countTxt;
        txt.onkeyup = countTxt1;
      }
      function countTxt(){
        var txt = document.getElementById("txt");
        var count = document.getElementById("count");
        count.innerHTML = 140-txt.value.length;
      }
      function countTxt1(){
        var txt = document.getElementById("txt");
        var count = document.getElementById("count");
        count.innerHTML = 140-txt.value.length;
      }
    </script>
   
  </head>

  <body onload="go()" id="bodytag"> 

 <div class="switlist">
<header class= "fixed" id="mainheader">
  <div class="row">
    <div class="left">
    <%= link_to swits_path,id: "alogo" do %>
      <%= image_tag "logo.png" %>
    <% end %>
    </div>
    <% if user_signed_in? %>
    

      <div class="right" style="margin-top:15px">
         <% if user_signed_in? %>

      <b style="font-size:20px; "><%= current_user.username%></b>

     <a> <%= link_to "(Sign out)", destroy_user_session_path, :method => :delete %></a>
      <% end %>
    <% @swit = Swit.new%>
    <%=render 'swits/form'%>
    </div>
     
     
      
    <%end%>
    </div>
</header>
</div>

<div class="row">

  
  
    <%= yield %>
    <%= javascript_include_tag "application" %>

 


  </div>

</div>

    

<div id="footer"> 

  </body>
</html>